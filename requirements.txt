# Extract & Load 파이프라인 런타임 의존성.
# Airflow는 docker-compose의 apache/airflow 이미지가 이미 포함하므로 여기선 제외
# (버전 충돌 방지 — Airflow 이미지에 얹을 추가 패키지만 명시).
pyarrow==18.1.0
psycopg2-binary==2.9.10
boto3==1.35.90
# DuckLake(Phase 5)는 DuckDB 확장이라 pip 패키지가 아니라 런타임에 INSTALL ducklake로
# 받는다. 그 확장을 지원하는 DuckDB 1.3+가 필요하다(로컬 실측은 1.5.4).
duckdb>=1.3.0

# dbt는 Phase 2에서 본격 사용. 구조만 잡아둔다.
dbt-core==1.9.1
dbt-duckdb==1.9.1
